@model IEnumerable<Clothes_shop.Models.Products>

@{
    Layout = "_Layout";
}

<div class="container-fluid px-4 py-5">
    <div class="d-flex justify-content-between align-items-end mb-4 pb-2 border-bottom">
        <div>
            <h1 class="fw-bolder display-6 text-dark mb-1">Quản Lý Sản Phẩm</h1>
        </div>
        <div>
            <a asp-area="Admin" asp-controller="Product" asp-action="Create" class="btn btn-primary btn-lg rounded-3 px-4 py-3 shadow d-flex align-items-center">
                <i class="bi bi-plus-circle-fill me-2"></i>
                <span class="fw-bold">THÊM MỚI SẢN PHẨM</span>
            </a>
        </div>
    </div>

    <div class="card border-0 shadow-sm rounded-4 overflow-hidden">
        <div class="table-responsive">
            <table class="table table-hover align-middle mb-0">
                <thead class="bg-light border-bottom">
                    <tr class="text-primary text-uppercase">
                        <th class="py-4 fw-bold">Tên Sản Phẩm</th>
                        <th class="py-4 fw-bold">Hình Ảnh</th>
                        <th class="py-4 fw-bold text-center">Giá Bán </th>
                        <th class="py-4 fw-bold">Mô Tả</th>
                        <th class="py-4 fw-bold">Số Lượng Tồn Kho</th>
                        <th class="py-4 fw-bold">Danh Mục</th>
                    </tr>
                </thead>
                <tbody class="border-top-0">
                    @if (Model != null && Model.Any())
                    {
                        foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    <span class="badge bg-light text-dark border px-3 py-2 fs-6">
                                        @item.Name
                                    </span>
                                </td>
                                <td>
                                    <span class="fw-bold text-dark">@item.ImageUrl</span>
                                </td>
                                <td class="text-center">
                                    <span class="badge rounded-pill bg-info text-dark px-3">
                                         @item.Price
                                    </span>
                                </td>
                                <td>
                                    <span class="badge bg-light text-dark border px-3 py-2 fs-6">
                                        @item.Description
                                    </span>
                                </td>
                                <td>
                                    <span class="badge bg-light text-dark border px-3 py-2 fs-6">
                                        @item.quantity
                                    </span>
                                </td>
                                <td>
                                    <span class="badge bg-light text-dark border px-3 py-2 fs-6">
                                        @item.Category
                                    </span>
                                </td>
                                <td class="pe-4 text-center">
                                    <div class="d-flex justify-content-center gap-2">
                                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-warning">
                                            <i class="bi bi-pencil"></i>
                                        </a>
                                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-outline-danger">
                                            <i class="bi bi-trash"></i>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="9" class="text-center py-4 text-muted">Chưa có sản phẩm nào</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
